name: 🗞 Market Sentinel — Daily Briefing

on:
  schedule:
    - cron: '50 21 * * 0-4'
  workflow_dispatch:
    inputs:
      note:
        description: '수동 실행 메모 (선택사항)'
        required: false
        default: '수동 실행'

jobs:
  briefing:
    name: Generate & Publish Briefing
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write

    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Install dependencies
        run: pip install -r requirements.txt

      - name: 🤖 Generate briefing
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        run: python generate.py

      - name: 💾 Commit & push docs
        run: |
          git config user.name  "Market Sentinel Bot"
          git config user.email "actions@github.com"
          git pull origin main --rebase
          git add docs/
          if git diff --staged --quiet; then
            echo "변경사항 없음 — 스킵"
          else
            DATE_KST=$(TZ='Asia/Seoul' date '+%Y-%m-%d %H:%M KST')
            git commit -m "📊 브리핑 발행 — ${DATE_KST} [skip ci]"
            git push
          fi
